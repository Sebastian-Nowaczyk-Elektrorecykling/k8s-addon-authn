# Generated. Secret material is resolved from the worker's environment.
version: 1
metadata:
  name: Cluster sites OIDC
entries:
  - model: authentik_providers_oauth2.oauth2provider
    id: cluster-sites-provider
    identifiers:
      name: cluster-sites
    attrs:
      client_id: cluster-sites
      client_secret: !Env OAUTH2_CLIENT_SECRET
      client_type: confidential
      # New authentik providers default to no grants; enable the browser flow.
      grant_types:
        - authorization_code
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: hashed_user_id
      issuer_mode: per_provider
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
      redirect_uris:
        - matching_mode: strict
          url: https://home.${DOMAIN}/oauth2/callback
        - matching_mode: strict
          url: https://authentik.${ADMIN_DOMAIN}/oauth2/callback
        - matching_mode: strict
          url: https://hubble.${ADMIN_DOMAIN}/oauth2/callback
        - matching_mode: strict
          url: https://longhorn.${ADMIN_DOMAIN}/oauth2/callback
        - matching_mode: strict
          url: https://openfga.${ADMIN_DOMAIN}/oauth2/callback
        - matching_mode: strict
          url: https://s3.${DOMAIN}/oauth2/callback
  - model: authentik_core.application
    identifiers:
      slug: cluster-sites
    attrs:
      name: Cluster sites
      provider: !KeyOf cluster-sites-provider
      meta_launch_url: https://home.${DOMAIN}
