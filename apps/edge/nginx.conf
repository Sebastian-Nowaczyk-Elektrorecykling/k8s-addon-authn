# Generated by scripts/generate.py. No credentials are stored here.
worker_processes auto;
pid /tmp/nginx.pid;
error_log /dev/stderr warn;
events { worker_connections 1024; }
http {
  access_log off;
  server_tokens off;
  client_body_temp_path /tmp/client;
  proxy_temp_path /tmp/proxy;
  fastcgi_temp_path /tmp/fastcgi;
  uwsgi_temp_path /tmp/uwsgi;
  scgi_temp_path /tmp/scgi;
  resolver ${CLUSTER_DNS_IP} valid=30s ipv6=off;
  map $http_upgrade $connection_upgrade { default upgrade; '' close; }
  map "$http_x_cluster_token$http_authorization" $machine_request { '' 0; default 1; }
  server {
    listen 8080 default_server;
    server_name _;
    location = /healthz { return 200 'ok'; }
    location / { return 404; }
  }
  server {
    listen 8080;
    server_name auth.${DOMAIN};
    # Login, OIDC and user self-service must work before a site grant exists.
    location ^~ /if/admin { return 404; }
    location / {
      set $idp http://authentik-server.authn.svc.cluster.local:80;
      proxy_pass $idp;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_http_version 1.1;
    }
  }
  server {
    listen 8080;
    server_name home.${DOMAIN};
    client_max_body_size 0;
    location ^~ /oauth2/ {
      proxy_pass http://127.0.0.1:4180;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Auth-Request-Redirect https://$host/;
      proxy_set_header Authorization "";
      proxy_set_header X-Cluster-Token "";
    }
    location = /authn/identity {
      proxy_pass http://127.0.0.1:9080/identity;
      proxy_set_header X-Original-Host $host;
      proxy_intercept_errors on;
      error_page 401 = @identity_login;
    }
    location @identity_login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/authn/identity;
    }
    location = /_authn_check {
      internal;
      proxy_pass http://127.0.0.1:9080/check;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";
      proxy_set_header X-Original-Host $host;
    }
    location @login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/;
    }
    location / {
      auth_request /_authn_check;
      auth_request_set $principal $upstream_http_x_cluster_principal;
      auth_request_set $app_cookie $upstream_http_x_upstream_cookie;
      error_page 401 = @login;
      set $backend http://heimdall.authn.svc.cluster.local:80;
      proxy_pass $backend;
      proxy_request_buffering off;
      proxy_buffering off;
      proxy_read_timeout 300s;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header X-Cluster-Principal $principal;
      proxy_set_header X-Forwarded-User $principal;
      proxy_set_header X-Auth-Request-User $principal;
      proxy_set_header X-Forwarded-Email "";
      proxy_set_header X-Forwarded-Groups "";
      proxy_set_header X-Forwarded-Preferred-Username "";
      proxy_set_header X-Forwarded-Access-Token "";
      proxy_set_header X-Auth-Request-Email "";
      proxy_set_header X-Auth-Request-Groups "";
      proxy_set_header X-Auth-Request-Access-Token "";
      proxy_set_header Remote-User "";
      proxy_set_header X-Cluster-Token "";
      proxy_set_header X-Original-Host "";
      proxy_set_header Authorization "";
      proxy_set_header Cookie $app_cookie;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }
  }
  server {
    listen 8080;
    server_name authentik.${ADMIN_DOMAIN};
    client_max_body_size 0;
    location ^~ /oauth2/ {
      proxy_pass http://127.0.0.1:4180;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Auth-Request-Redirect https://$host/;
      proxy_set_header Authorization "";
      proxy_set_header X-Cluster-Token "";
    }
    location = /authn/identity {
      proxy_pass http://127.0.0.1:9080/identity;
      proxy_set_header X-Original-Host $host;
      proxy_intercept_errors on;
      error_page 401 = @identity_login;
    }
    location @identity_login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/authn/identity;
    }
    location = /_authn_check {
      internal;
      proxy_pass http://127.0.0.1:9080/check;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";
      proxy_set_header X-Original-Host $host;
    }
    location @login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/;
    }
    location / {
      auth_request /_authn_check;
      auth_request_set $principal $upstream_http_x_cluster_principal;
      auth_request_set $app_cookie $upstream_http_x_upstream_cookie;
      error_page 401 = @login;
      set $backend http://authentik-server.authn.svc.cluster.local:80;
      proxy_pass $backend;
      proxy_request_buffering off;
      proxy_buffering off;
      proxy_read_timeout 300s;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header X-Cluster-Principal $principal;
      proxy_set_header X-Forwarded-User $principal;
      proxy_set_header X-Auth-Request-User $principal;
      proxy_set_header X-Forwarded-Email "";
      proxy_set_header X-Forwarded-Groups "";
      proxy_set_header X-Forwarded-Preferred-Username "";
      proxy_set_header X-Forwarded-Access-Token "";
      proxy_set_header X-Auth-Request-Email "";
      proxy_set_header X-Auth-Request-Groups "";
      proxy_set_header X-Auth-Request-Access-Token "";
      proxy_set_header Remote-User "";
      proxy_set_header X-Cluster-Token "";
      proxy_set_header X-Original-Host "";
      proxy_set_header Authorization "";
      proxy_set_header Cookie $app_cookie;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }
  }
  server {
    listen 8080;
    server_name hubble.${ADMIN_DOMAIN};
    client_max_body_size 0;
    location ^~ /oauth2/ {
      proxy_pass http://127.0.0.1:4180;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Auth-Request-Redirect https://$host/;
      proxy_set_header Authorization "";
      proxy_set_header X-Cluster-Token "";
    }
    location = /authn/identity {
      proxy_pass http://127.0.0.1:9080/identity;
      proxy_set_header X-Original-Host $host;
      proxy_intercept_errors on;
      error_page 401 = @identity_login;
    }
    location @identity_login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/authn/identity;
    }
    location = /_authn_check {
      internal;
      proxy_pass http://127.0.0.1:9080/check;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";
      proxy_set_header X-Original-Host $host;
    }
    location @login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/;
    }
    location / {
      auth_request /_authn_check;
      auth_request_set $principal $upstream_http_x_cluster_principal;
      auth_request_set $app_cookie $upstream_http_x_upstream_cookie;
      error_page 401 = @login;
      set $backend http://hubble-ui.kube-system.svc.cluster.local:80;
      proxy_pass $backend;
      proxy_request_buffering off;
      proxy_buffering off;
      proxy_read_timeout 300s;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header X-Cluster-Principal $principal;
      proxy_set_header X-Forwarded-User $principal;
      proxy_set_header X-Auth-Request-User $principal;
      proxy_set_header X-Forwarded-Email "";
      proxy_set_header X-Forwarded-Groups "";
      proxy_set_header X-Forwarded-Preferred-Username "";
      proxy_set_header X-Forwarded-Access-Token "";
      proxy_set_header X-Auth-Request-Email "";
      proxy_set_header X-Auth-Request-Groups "";
      proxy_set_header X-Auth-Request-Access-Token "";
      proxy_set_header Remote-User "";
      proxy_set_header X-Cluster-Token "";
      proxy_set_header X-Original-Host "";
      proxy_set_header Authorization "";
      proxy_set_header Cookie $app_cookie;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }
  }
  server {
    listen 8080;
    server_name longhorn.${ADMIN_DOMAIN};
    client_max_body_size 0;
    location ^~ /oauth2/ {
      proxy_pass http://127.0.0.1:4180;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Auth-Request-Redirect https://$host/;
      proxy_set_header Authorization "";
      proxy_set_header X-Cluster-Token "";
    }
    location = /authn/identity {
      proxy_pass http://127.0.0.1:9080/identity;
      proxy_set_header X-Original-Host $host;
      proxy_intercept_errors on;
      error_page 401 = @identity_login;
    }
    location @identity_login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/authn/identity;
    }
    location = /_authn_check {
      internal;
      proxy_pass http://127.0.0.1:9080/check;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";
      proxy_set_header X-Original-Host $host;
    }
    location @login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/;
    }
    location / {
      auth_request /_authn_check;
      auth_request_set $principal $upstream_http_x_cluster_principal;
      auth_request_set $app_cookie $upstream_http_x_upstream_cookie;
      error_page 401 = @login;
      set $backend http://longhorn-frontend.longhorn-system.svc.cluster.local:80;
      proxy_pass $backend;
      proxy_request_buffering off;
      proxy_buffering off;
      proxy_read_timeout 300s;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header X-Cluster-Principal $principal;
      proxy_set_header X-Forwarded-User $principal;
      proxy_set_header X-Auth-Request-User $principal;
      proxy_set_header X-Forwarded-Email "";
      proxy_set_header X-Forwarded-Groups "";
      proxy_set_header X-Forwarded-Preferred-Username "";
      proxy_set_header X-Forwarded-Access-Token "";
      proxy_set_header X-Auth-Request-Email "";
      proxy_set_header X-Auth-Request-Groups "";
      proxy_set_header X-Auth-Request-Access-Token "";
      proxy_set_header Remote-User "";
      proxy_set_header X-Cluster-Token "";
      proxy_set_header X-Original-Host "";
      proxy_set_header Authorization "";
      proxy_set_header Cookie $app_cookie;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }
  }
  server {
    listen 8080;
    server_name openfga.${ADMIN_DOMAIN};
    client_max_body_size 0;
    location ^~ /oauth2/ {
      proxy_pass http://127.0.0.1:4180;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Auth-Request-Redirect https://$host/;
      proxy_set_header Authorization "";
      proxy_set_header X-Cluster-Token "";
    }
    location = /authn/identity {
      proxy_pass http://127.0.0.1:9080/identity;
      proxy_set_header X-Original-Host $host;
      proxy_intercept_errors on;
      error_page 401 = @identity_login;
    }
    location @identity_login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/authn/identity;
    }
    location = /_authn_check {
      internal;
      proxy_pass http://127.0.0.1:9080/check;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";
      proxy_set_header X-Original-Host $host;
    }
    location @login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/;
    }
    location / {
      auth_request /_authn_check;
      auth_request_set $principal $upstream_http_x_cluster_principal;
      auth_request_set $app_cookie $upstream_http_x_upstream_cookie;
      error_page 401 = @login;
      set $backend http://openfga.authn.svc.cluster.local:8080;
      proxy_pass $backend;
      proxy_request_buffering off;
      proxy_buffering off;
      proxy_read_timeout 300s;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header X-Cluster-Principal $principal;
      proxy_set_header X-Forwarded-User $principal;
      proxy_set_header X-Auth-Request-User $principal;
      proxy_set_header X-Forwarded-Email "";
      proxy_set_header X-Forwarded-Groups "";
      proxy_set_header X-Forwarded-Preferred-Username "";
      proxy_set_header X-Forwarded-Access-Token "";
      proxy_set_header X-Auth-Request-Email "";
      proxy_set_header X-Auth-Request-Groups "";
      proxy_set_header X-Auth-Request-Access-Token "";
      proxy_set_header Remote-User "";
      proxy_set_header X-Cluster-Token "";
      proxy_set_header X-Original-Host "";
      proxy_set_header Authorization $http_authorization;
      proxy_set_header Cookie $app_cookie;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }
  }
  server {
    listen 8080;
    server_name s3.${DOMAIN};
    client_max_body_size 0;
    location ^~ /oauth2/ {
      proxy_pass http://127.0.0.1:4180;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Auth-Request-Redirect https://$host/;
      proxy_set_header Authorization "";
      proxy_set_header X-Cluster-Token "";
    }
    location = /authn/identity {
      proxy_pass http://127.0.0.1:9080/identity;
      proxy_set_header X-Original-Host $host;
      proxy_intercept_errors on;
      error_page 401 = @identity_login;
    }
    location @identity_login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/authn/identity;
    }
    location = /_authn_check {
      internal;
      proxy_pass http://127.0.0.1:9080/check;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";
      proxy_set_header X-Original-Host $host;
    }
    location @login {
      if ($machine_request) { return 401; }
      return 302 https://$host/oauth2/start?rd=/;
    }
    location / {
      auth_request /_authn_check;
      auth_request_set $principal $upstream_http_x_cluster_principal;
      auth_request_set $app_cookie $upstream_http_x_upstream_cookie;
      error_page 401 = @login;
      set $backend http://garage.garage.svc.cluster.local:3900;
      proxy_pass $backend;
      proxy_request_buffering off;
      proxy_buffering off;
      proxy_read_timeout 300s;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header X-Cluster-Principal $principal;
      proxy_set_header X-Forwarded-User $principal;
      proxy_set_header X-Auth-Request-User $principal;
      proxy_set_header X-Forwarded-Email "";
      proxy_set_header X-Forwarded-Groups "";
      proxy_set_header X-Forwarded-Preferred-Username "";
      proxy_set_header X-Forwarded-Access-Token "";
      proxy_set_header X-Auth-Request-Email "";
      proxy_set_header X-Auth-Request-Groups "";
      proxy_set_header X-Auth-Request-Access-Token "";
      proxy_set_header Remote-User "";
      proxy_set_header X-Cluster-Token "";
      proxy_set_header X-Original-Host "";
      proxy_set_header Authorization $http_authorization;
      proxy_set_header Cookie $app_cookie;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }
  }
}
